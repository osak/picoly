Picoly - A lightweight Kanban Board system for AI agents
--------------------------------------------------------

# プロジェクトの目的
PicolyはAIがサブタスクの一覧を管理するためのKanban Boardシステムです。このシステムの大きな目的は以下の2つです。

* Claude Code等のコーディングエージェントが、タスクリストをsubagentと共有し、一貫性のある形で管理できるようにすること
* 作業の進捗状況を人間に分かりやすく可視化すること

# データ構成
## チケット
チケットは以下の要素からなる、実行されるべきタスクの情報を構造化したデータ概念です。

- 自動発行ID
- タイトル
- 詳細説明
- ステータス
- コメント（進捗報告）

## ユーザー
Picolyにはユーザーの概念があります。チケットを作成したり、コメントを書いたりすると操作を行ったユーザー名も記録されます。
ユーザーは `PICOLY_USER_ID` 環境変数を参照して決定します。

## ロール
Picolyには3つのロールがあります。それぞれのロールには複数のユーザーが割り当てられている可能性があります。
ロールによって行える操作に制限があります。

### 神ロール
神ロールはPicoly上のすべての操作を行うことができます。

### 管理者ロール
管理者ロールはPicoly上のほとんどの操作を行なえますが、削除に関連する操作には制限がかかっています。
Version 1では神ロールと同等の権限があります。

### 作業者ロール
作業者ロールはチケットのステータス変更とコメント追加を行うことができます。その他の編集は行えません。

# データ実装
## DB
データベースとしてはプロジェクト固有のsqlite3 DBを使います。また、更新のたびにチケットの内容はHuman-readableなテキストとしてMarkdownファイルへ出力されます。
ファイルのフォーマットはテンプレートによって定められます。

# CLIコマンド
Picolyの操作はすべて `picoly` CLIコマンドを通じて行います。特にオプションで指定されない場合、コマンドの出力は単一のJSONオブジェクトです。

**特に指定されていない限り、操作はアトミックです。** 複数のエージェントが `picoly` コマンドを同時に実行した場合、ロックを介して一つずつ順番に処理されます。
ある `picoly` コマンドが書き込み操作しようとしたチケットが実行時点よりあとに他のコマンドによって更新される、いわゆるレースコンディションが発生した場合には一律でエラーになります。
読み込みのみの操作はレースコンディションを引き起こしません。

## チケットの追加
`picoly add` コマンドでチケットを追加することができます。チケットのパラメータはコマンドラインオプションか、等価なJSONで指定することができます。
このコマンドは管理者ロールのエージェントにのみ実行が許されています。

## チケットの編集
`picoly edit` コマンドでチケットのタイトルと詳細説明を編集することができます。
このコマンドは管理者ロールのエージェントにのみ実行が許されています。

## チケットの状態更新
`picoly work` コマンドでステータスの変更とコメントの追加ができます。両方同時に指定することもできます。

## チケットの取得
`picoly read` コマンドでチケットの完全な情報を取得できます。

## チケットの一覧
`picoly list` コマンドでチケットの一覧を取得できます。デフォルトではチケットID、タイトル、ステータス、コメント数、最終更新日時が含まれます。
オプションでソートしたり、特定のステータスでフィルタしたりすることができます。

# 実装のポイント
* Go 1.25を利用する。
* テストを書く。
* 可能な限り外部ライブラリは使わず、可能な限り標準ライブラリを利用する。特筆すべき例外は以下。
  * sqlite3
* JSONのシリアライズ・デシリアライズにはjsonv2を利用する。
  * spec: https://go.dev/blog/jsonv2-exp
  * ビルド時に `GOEXPERIMENT=jsonv2` 環境変数が必要になる。
* ロギングにはslog標準ライブラリを利用する。
* タスクランナーにはJustを利用する。
  * 再現性の確保、および環境構築の手間を減らすためJustのバイナリをレポジトリにコミットしてください。
